import 'package:flutter/material.dart';

import '../../shared/placeholder_feature_page.dart';

class CrisisPage extends StatelessWidget {
  const CrisisPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderFeaturePage(
      title: 'Crisis Handbook',
      description:
          'Port function-calling logic and local safety resources. Keep emergency messaging '
          'prominent and available offline.',
    );
  }
}
